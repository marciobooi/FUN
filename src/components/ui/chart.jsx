/**
 * @typedef {Object.<string, {
 *   label?: string,
 *   color?: string,
 *   theme?: { light?: string, dark?: string }
 * }>} ChartConfig
 */

export {
  ChartContainer,
  ChartLegend,
  ChartLegendContent,
  ChartTooltip,
  ChartTooltipContent,
} from './ChartContainer'
